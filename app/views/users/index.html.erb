<p id="notice"><%= notice %></p>

<% provide(:title, 'All users') %>
<h1>All Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <%= will_paginate %>

  <tbody>
    <%= render @users %>
  </tbody>

</table>

<%= will_paginate %>

<br>

<%= link_to 'New User', new_user_path %>
